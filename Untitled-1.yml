---
moriarty:
  children:

    # --- Database topology ---
    db_primary:
      hosts:
        db-primary:
          ansible_host: 10.128.0.79
          server_id: 1
          db_host: "10.128.0.79"

    db_replicas:
      hosts:
        db-replica-1:
          ansible_host: 10.128.0.71   
          server_id: 2
          db_host: "10.128.0.71"

    dbs:
      children:
        db_primary:
        db_replicas:

    runtime:
      hosts:
        web-1:
          ansible_host: 10.128.0.76
        web-2:
          ansible_host: 10.128.0.77
        web-3:
          ansible_host: 10.128.0.78

  # --- Shared vars ---
  vars:
    ansible_port: 22
    ansible_user: ansible

    drush_target_host: 10.128.0.76

    db_name: "moriaty"
    db_user: "devportal"
    db_password: "pass"
    metabase_db_name: "metabase"

    repl_password: "pass"

    # Convenience vars for settings.php template
    db_host_primary:  "10.128.0.79"
    db_host_replica1: "10.128.0.71"
