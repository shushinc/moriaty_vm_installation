---
moriarty:
  children:

    # --- Database topology ---
    db_primary:
      hosts:
        db-primary:
          ansible_host: 10.128.0.36
          server_id: 1
          db_host: "10.128.0.36"

    db_replicas:
      hosts:
        db-replica-1:
          ansible_host: 10.128.0.23
          server_id: 2
          db_host: "10.128.0.23"

    dbs:
      children:
        db_primary: 
        db_replicas: 

    runtime:
      hosts:
        web-1:
          ansible_host: 10.128.0.35
          is_drush_target: true
        web-2:
          ansible_host: 10.128.0.36
          is_drush_target: false


  # --- Shared vars ---
  vars:
    ansible_port: 22
    ansible_user: ansible

    db_name: "moriaty"
    db_user: "devportal"
    db_password: "pass"
      #metabase_db_name: "metabase"

    repl_password: "pass"

    # Convenience vars for settings.php template
    db_host_primary:  "10.128.0.36"
    db_host_replica1: "10.128.0.23"
