---
moriarty:
  children:
    dbs:
      hosts:
        10.128.0.8:
          db_host: "10.128.0.8" 
    runtime:
      hosts:
        10.128.0.7:

  # Required Vars
  vars:
    ansible_port: 22
    ansible_user: ansible
    drush_target_host: 10.128.0.7